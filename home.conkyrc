conky.config = {
	out_to_console = true,
	out_to_x = false,
	background = false,
	update_interval = 1,
	total_run_times = 0,
	use_spacer = 'left'
}

conky.text = [[
	cpu ${cpu 0}% ${freq 0}MHz ${acpitemp}C | mem ${memperc}% | rd ${diskio_read} wr ${diskio_write} | ${addr lagg0} | D:${downspeed lagg0}/s U:${upspeed lagg0}/s | vol ${mixer}% | ${battery} ${battery_time} | ${time %a %b %d %I:%M:%S %p}
]]
