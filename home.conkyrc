conky.config = {
	out_to_console = true,
	out_to_x = false,
	background = false,
	update_interval = 1,
	total_run_times = 0,
	use_spacer = 'left'
}

conky.text = [[
cpu ${cpu 0}% ${freq 0}MHz ${acpitemp}C | mem ${memperc}% | r ${diskio_read} w ${diskio_write} | ${addr lagg0} | d ${downspeed lagg0}/s u ${upspeed lagg0}/s | vol ${mixer}% | ${battery} ${battery_time} | ${time %a %b %d %H:%M:%S}
]]
